# frontend

Not built yet. This is a placeholder — no React app is scaffolded here.

Per plan: the backend (`../backend/`) is developed first against the CLI and
plain data files. This folder gets a real React app (Vite, standard CRA-like
structure: `src/`, `public/`, `package.json`) only when we reach PLAN.md item
10 (Web UI), or when explicitly asked for sooner.

Don't add framework files, `package.json`, or dependencies here ahead of
that point.
